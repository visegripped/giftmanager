# Environment Variables Template
# Copy this file to .env.local for local development
# These variables are used by docker-compose.yml

# Port Configuration (configured to avoid conflicts with other projects)
# GiftManager uses: 8081, 3307, 8082, 5174
# Other projects can use standard ports: 80, 3306, 8080, 5173
PHP_PORT=8081
MYSQL_PORT=3307
PHPMYADMIN_PORT=8082
FRONTEND_PORT=5174

# Database Configuration
DB_HOST=mysql
DB_NAME=giftmanager
DB_USER=visegripped
DB_PASSWORD=generate_secure_password_here

# Reporting Database
REPORT_DB_NAME=reports
REPORT_DB_USER=visegripped
REPORT_DB_PASSWORD=generate_secure_password_here

# MySQL Root Password
MYSQL_ROOT_PASSWORD=generate_secure_root_password_here

# Google OAuth
VITE_GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_OAUTH_CLIENT_ID=your_google_client_id_here

# Facebook OAuth (requires HTTPS - see README for SSL certificate setup)
VITE_FB_APP_ID=your_facebook_app_id_here
FB_APP_ID=your_facebook_app_id_here

# Application Environment
APP_ENV=development

# Note: API URLs are protocol-relative (//) to support both HTTP and HTTPS
# The app will automatically use the same protocol as the current page

